use std::{
    env,
    error::Error,
    
fn main() {
    let args: Vec<string> = env: :args() .collect();
    
    if arge.len() !=3 {
        println!("Usage:");
        println!("sha1_cracker: <wordlist.txt> <sha1_hash>");
        return;
    }
}